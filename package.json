{
  "name": "@karah/shared",
  "version": "1.0.0",
  "description": "Moteur de validation et de logique conditionnelle partagé entre l'API, le web et le mobile.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Infinite-Solutions-Intl/karah-remix-shared.git"
  },
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.test.json && node .test-build/tests/run.js",
    "prepare": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
